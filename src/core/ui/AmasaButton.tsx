import { Button, styled } from 'tamagui'

export const AmasaButton = styled(Button, {
  name: 'AmasaButton',
  backgroundColor: '$primary',
  color: 'white',
  hoverStyle: {
    backgroundColor: '$primaryDark',
  },
})
